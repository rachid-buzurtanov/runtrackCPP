class Joueur {
    public:
        ~Joueur();
        double x;
        double y;
        void initialize(double initialX, double initialY);
        void setX(double newX);
        void setY(double setY);
};
